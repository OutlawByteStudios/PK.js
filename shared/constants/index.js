const assignPermissionCheck = require('./permissions/assign-permission-check');
const gamePermissions = require('./permissions/game-permissions');
const panelPermissions = require('./permissions/panel-permissions');
const permissionPresets = require('./permissions/permission-presets');

module.exports = {
  assignPermissionCheck,
  gamePermissions,
  panelPermissions,
  permissionPresets
};