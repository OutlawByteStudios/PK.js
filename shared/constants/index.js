const gamePermissions = require('./game-permissions');
const panelPermissions = require('./panel-permissions');
const permissionPresets = require('./permission-presets');

module.exports = {
  gamePermissions,
  panelPermissions,
  permissionPresets
};